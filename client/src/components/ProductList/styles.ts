import { makeStyles } from 'tss-react/mui';

export default makeStyles()(() => ({
    productList: {
        display: 'flex',
        flexWrap: 'wrap',
        gap: '8px',
        marginTop: '20px',
    },
}));
